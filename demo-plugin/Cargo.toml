[package]
name = "demo-plugin"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
sha2 = "0.10"

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"

[workspace]
