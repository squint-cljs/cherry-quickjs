--- a/grenadine/version.cljc
+++ b/grenadine/version.cljc
@@ -41,7 +41,7 @@
 
 (defn- digit?
   [c]
-  (let [n (int c)]
+  (let [n (if (string? c) (.charCodeAt c 0) (int c))]
     (<= 48 n 57)))
 
 (defn- normalize-number
